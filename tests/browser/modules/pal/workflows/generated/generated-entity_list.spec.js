/** Auto-generated by ARK Workbench Scanner */
var { test, expect } = require('../../../../WorkbenchFixture');
var execSync = require('child_process').execSync;
var path = require('path');
var SEED = path.resolve(__dirname, '../../../../../pal/pal_seed_interactive.php');
var TENANT = process.env.PAL_TEST_TENANT || '502';

async function goTo(page, url) {
    await page.goto(url);
    await page.waitForSelector('[data-wb-component="app-shell"]', { timeout: 15000 });
}

test.describe('pal:generated-entity_list', function () {
    test.afterAll(function () { try { execSync('php ' + SEED + ' --cleanup --tenant=' + TENANT, { timeout: 10000 }); } catch (e) {} });

    test('Project - Job Order', async function ({ page, integrity }) {
        test.setTimeout(30000);
        var base = '/admin/project-audit-ledger';
        var seed, pid;
        var out = execSync('php ' + SEED + ' --tenant=' + TENANT, { encoding: 'utf-8', timeout: 15000 });
        seed = JSON.parse(out);
        if (!seed.ok) { integrity.gap('Seed failed'); return; }
        pid = seed.project_id;
        if (!pid) { integrity.gap('No entity ID'); return; }
        await goTo(page, base + '/projects');
        var listEl = page.locator('[data-ikb-list], [data-wb-list]').first();
        if (await listEl.count() === 0) { integrity.gap('No list for pal.project'); return; }
        var cnt = await listEl.locator('tbody tr').count();
        if (cnt === 0) { integrity.gap('List empty for pal.project'); }
        else { console.log('  projects list: ' + cnt + ' rows'); }
    });

    test('Approval Request', async function ({ page, integrity }) {
        test.setTimeout(30000);
        var base = '/admin/project-audit-ledger';
        var seed, pid;
        var out = execSync('php ' + SEED + ' --tenant=' + TENANT, { encoding: 'utf-8', timeout: 15000 });
        seed = JSON.parse(out);
        if (!seed.ok) { integrity.gap('Seed failed'); return; }
        pid = seed.project_id;
        if (!pid) { integrity.gap('No entity ID'); return; }
        await goTo(page, base + '/approvals');
        var listEl = page.locator('[data-ikb-list], [data-wb-list]').first();
        if (await listEl.count() === 0) { integrity.gap('No list for pal.approval'); return; }
        var cnt = await listEl.locator('tbody tr').count();
        if (cnt === 0) { integrity.gap('List empty for pal.approval'); }
        else { console.log('  approvals list: ' + cnt + ' rows'); }
    });

    test('Client', async function ({ page, integrity }) {
        test.setTimeout(30000);
        var base = '/admin/project-audit-ledger';
        var seed, pid;
        var out = execSync('php ' + SEED + ' --tenant=' + TENANT, { encoding: 'utf-8', timeout: 15000 });
        seed = JSON.parse(out);
        if (!seed.ok) { integrity.gap('Seed failed'); return; }
        pid = seed.client_id;
        if (!pid) { integrity.gap('No entity ID'); return; }
        await goTo(page, base + '/clients');
        var listEl = page.locator('[data-ikb-list], [data-wb-list]').first();
        if (await listEl.count() === 0) { integrity.gap('No list for pal.client'); return; }
        var cnt = await listEl.locator('tbody tr').count();
        if (cnt === 0) { integrity.gap('List empty for pal.client'); }
        else { console.log('  clients list: ' + cnt + ' rows'); }
    });

});