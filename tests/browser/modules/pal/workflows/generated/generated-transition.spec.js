/** Auto-generated by ARK Workbench Scanner */
var { test, expect } = require('../../../../WorkbenchFixture');
var execSync = require('child_process').execSync;
var path = require('path');
var SEED = path.resolve(__dirname, '../../../../../pal/pal_seed_interactive.php');
var TENANT = process.env.PAL_TEST_TENANT || '502';

async function goTo(page, url) {
    await page.goto(url);
    await page.waitForSelector('[data-wb-component="app-shell"]', { timeout: 15000 });
}

test.describe('pal:generated-transition', function () {
    test.afterAll(function () { try { execSync('php ' + SEED + ' --cleanup --tenant=' + TENANT, { timeout: 10000 }); } catch (e) {} });

    test('draftpending', async function ({ page, integrity }) {
        test.setTimeout(30000);
        var base = '/admin/project-audit-ledger';
        var seed, pid;
        var out = execSync('php ' + SEED + ' --tenant=' + TENANT, { encoding: 'utf-8', timeout: 15000 });
        seed = JSON.parse(out);
        if (!seed.ok) { integrity.gap('Seed failed'); return; }
        pid = seed.project_id;
        if (!pid) { integrity.gap('No entity ID'); return; }
        var result = await page.evaluate(async function(opts) {
            var fd = new URLSearchParams();
            fd.append('status', opts.toStatus);
            var r = await fetch('/api/v1/project-audit-ledger/projects/' + opts.pid + '/status',
                { method: 'POST', body: fd, headers: { 'Content-Type': 'application/x-www-form-urlencoded' } });
            return { ok: (await r.json()).ok, http: r.status };
        }, { pid: pid, toStatus: 'pending' });
        if (!result.ok) {
            integrity.gap('Transition draft->pending HTTP ' + result.http);
        } else {
            await goTo(page, base + '/projects/' + pid + '?disyl_nocache=1&wb_inspect=1');
            var m = await page.evaluate(function () { return window.__wbManifest; });
            if (m && m.status !== 'pending') { integrity.gap('Expected pending got ' + (m ? m.status : null)); }
            else { console.log('  draft->pending: ' + (m ? m.status : null)); }
        }
    });

    test('pendingapproved', async function ({ page, integrity }) {
        test.setTimeout(30000);
        var base = '/admin/project-audit-ledger';
        var seed, pid;
        var out = execSync('php ' + SEED + ' --tenant=' + TENANT, { encoding: 'utf-8', timeout: 15000 });
        seed = JSON.parse(out);
        if (!seed.ok) { integrity.gap('Seed failed'); return; }
        pid = seed.project_id;
        if (!pid) { integrity.gap('No entity ID'); return; }
        var result = await page.evaluate(async function(opts) {
            var fd = new URLSearchParams();
            fd.append('status', opts.toStatus);
            var r = await fetch('/api/v1/project-audit-ledger/projects/' + opts.pid + '/status',
                { method: 'POST', body: fd, headers: { 'Content-Type': 'application/x-www-form-urlencoded' } });
            return { ok: (await r.json()).ok, http: r.status };
        }, { pid: pid, toStatus: 'approved' });
        if (!result.ok) {
            integrity.gap('Transition pending->approved HTTP ' + result.http);
        } else {
            await goTo(page, base + '/projects/' + pid + '?disyl_nocache=1&wb_inspect=1');
            var m = await page.evaluate(function () { return window.__wbManifest; });
            if (m && m.status !== 'approved') { integrity.gap('Expected approved got ' + (m ? m.status : null)); }
            else { console.log('  pending->approved: ' + (m ? m.status : null)); }
        }
    });

    test('approvedstarted', async function ({ page, integrity }) {
        test.setTimeout(30000);
        var base = '/admin/project-audit-ledger';
        var seed, pid;
        var out = execSync('php ' + SEED + ' --tenant=' + TENANT, { encoding: 'utf-8', timeout: 15000 });
        seed = JSON.parse(out);
        if (!seed.ok) { integrity.gap('Seed failed'); return; }
        pid = seed.project_id;
        if (!pid) { integrity.gap('No entity ID'); return; }
        var result = await page.evaluate(async function(opts) {
            var fd = new URLSearchParams();
            fd.append('status', opts.toStatus);
            var r = await fetch('/api/v1/project-audit-ledger/projects/' + opts.pid + '/status',
                { method: 'POST', body: fd, headers: { 'Content-Type': 'application/x-www-form-urlencoded' } });
            return { ok: (await r.json()).ok, http: r.status };
        }, { pid: pid, toStatus: 'started' });
        if (!result.ok) {
            integrity.gap('Transition approved->started HTTP ' + result.http);
        } else {
            await goTo(page, base + '/projects/' + pid + '?disyl_nocache=1&wb_inspect=1');
            var m = await page.evaluate(function () { return window.__wbManifest; });
            if (m && m.status !== 'started') { integrity.gap('Expected started got ' + (m ? m.status : null)); }
            else { console.log('  approved->started: ' + (m ? m.status : null)); }
        }
    });

    test('startedongoing', async function ({ page, integrity }) {
        test.setTimeout(30000);
        var base = '/admin/project-audit-ledger';
        var seed, pid;
        var out = execSync('php ' + SEED + ' --tenant=' + TENANT, { encoding: 'utf-8', timeout: 15000 });
        seed = JSON.parse(out);
        if (!seed.ok) { integrity.gap('Seed failed'); return; }
        pid = seed.project_id;
        if (!pid) { integrity.gap('No entity ID'); return; }
        var result = await page.evaluate(async function(opts) {
            var fd = new URLSearchParams();
            fd.append('status', opts.toStatus);
            var r = await fetch('/api/v1/project-audit-ledger/projects/' + opts.pid + '/status',
                { method: 'POST', body: fd, headers: { 'Content-Type': 'application/x-www-form-urlencoded' } });
            return { ok: (await r.json()).ok, http: r.status };
        }, { pid: pid, toStatus: 'ongoing' });
        if (!result.ok) {
            integrity.gap('Transition started->ongoing HTTP ' + result.http);
        } else {
            await goTo(page, base + '/projects/' + pid + '?disyl_nocache=1&wb_inspect=1');
            var m = await page.evaluate(function () { return window.__wbManifest; });
            if (m && m.status !== 'ongoing') { integrity.gap('Expected ongoing got ' + (m ? m.status : null)); }
            else { console.log('  started->ongoing: ' + (m ? m.status : null)); }
        }
    });

    test('ongoingcompleted', async function ({ page, integrity }) {
        test.setTimeout(30000);
        var base = '/admin/project-audit-ledger';
        var seed, pid;
        var out = execSync('php ' + SEED + ' --tenant=' + TENANT, { encoding: 'utf-8', timeout: 15000 });
        seed = JSON.parse(out);
        if (!seed.ok) { integrity.gap('Seed failed'); return; }
        pid = seed.project_id;
        if (!pid) { integrity.gap('No entity ID'); return; }
        var result = await page.evaluate(async function(opts) {
            var fd = new URLSearchParams();
            fd.append('status', opts.toStatus);
            var r = await fetch('/api/v1/project-audit-ledger/projects/' + opts.pid + '/status',
                { method: 'POST', body: fd, headers: { 'Content-Type': 'application/x-www-form-urlencoded' } });
            return { ok: (await r.json()).ok, http: r.status };
        }, { pid: pid, toStatus: 'completed' });
        if (!result.ok) {
            integrity.gap('Transition ongoing->completed HTTP ' + result.http);
        } else {
            await goTo(page, base + '/projects/' + pid + '?disyl_nocache=1&wb_inspect=1');
            var m = await page.evaluate(function () { return window.__wbManifest; });
            if (m && m.status !== 'completed') { integrity.gap('Expected completed got ' + (m ? m.status : null)); }
            else { console.log('  ongoing->completed: ' + (m ? m.status : null)); }
        }
    });

});