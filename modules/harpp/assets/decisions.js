document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('decision-filters'),list=document.getElementById('decision-list'),status=document.getElementById('decision-status');async function load(){const query=new URLSearchParams(new FormData(form));for(const [k,v] of [...query])if(!v)query.delete(k);try{const rows=(await Harpp.fetch('/api/v1/harpp/decisions?'+query)).data.decisions||[];list.replaceChildren();for(const row of rows){const card=document.createElement('a');card.className='panel';card.href=`/harpp/decisions/${row.id}`;const h=document.createElement('h3');h.textContent=row.title;const p=document.createElement('p');p.className='muted';p.textContent=`${row.lifecycle_state} · ${row.priority} · ${row.decision_key}`;card.append(h,p);list.append(card);}if(!rows.length)list.textContent='No decisions match these filters.';}catch(e){status.textContent=e.message;}}form.onsubmit=e=>{e.preventDefault();load();};load();});
