document.addEventListener('DOMContentLoaded',()=>{const list=document.getElementById('notification-list'),status=document.getElementById('notification-status');let rows=[];function target(n){return n.decision_id?`/harpp/decisions/${n.decision_id}`:(n.conversation_id?`/harpp?conversation=${n.conversation_id}`:'/harpp/notifications');}async function load(){try{rows=(await Harpp.fetch('/api/v1/harpp/notifications?limit=100')).data.notifications||[];list.replaceChildren();for(const n of rows){const card=document.createElement('a');card.className='panel';card.href=target(n);let payload={};try{payload=JSON.parse(n.payload||'{}');}catch(_){}card.textContent=`${n.read_at?'':'● '}${payload.title||n.notification_type} — ${payload.event||n.status} (${n.created_at})`;card.onclick=()=>{if(!n.read_at)Harpp.fetch(`/api/v1/harpp/notifications/${n.id}/read`,{method:'POST'}).catch(()=>{});};list.append(card);}if(!rows.length)list.textContent='No notifications.';}catch(e){status.textContent=e.message;}}document.getElementById('mark-all').onclick=async()=>{try{await Promise.all(rows.filter(n=>!n.read_at).map(n=>Harpp.fetch(`/api/v1/harpp/notifications/${n.id}/read`,{method:'POST'})));await load();Harpp.pollUnread();}catch(e){status.textContent=e.message;}};load();});
