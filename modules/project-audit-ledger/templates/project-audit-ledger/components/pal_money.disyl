{* pal_money — delegates to workbench:money. *}
{include "workbench:money"
    formatted='₱' ~ (_m_amount|default:0|number_format:2)
    is_negative=(_m_amount|default:0 < 0)
    emphasis='primary'
}
