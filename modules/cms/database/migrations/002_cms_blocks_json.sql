-- CMS Module — Phase 2 Block Editor MVP
-- Add blocks_json storage on cms_content
-- NOTE: blocks_json is already included in 001_cms_content_core.sql CREATE TABLE.
-- This migration is kept as a no-op for backward compatibility with existing installs.

SET @dummy = 1;
