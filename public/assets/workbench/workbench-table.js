/**
 * ARK Workbench v0.1 — Responsive Table
 *
 * Enhances semantic tables with data-label mobile card mode.
 * No custom grid — uses native <table> semantics.
 */
(function () {
    'use strict';
    // Responsive table behavior is CSS-only via data-label attributes.
    // This module is reserved for future enhancements (sort, bulk actions).
})();
